# Frontend Guidelines

Applies to files in `src/`.

- Use helper functions instead of computations in templates — templates are for representation, not logic
- Keep components focused on rendering; move business logic to utils
